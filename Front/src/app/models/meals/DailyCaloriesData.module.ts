// src/app/models/daily-calories.model.ts

export interface DailyCaloriesData {
    date: string;  // The date as a string, e.g., "2025-04-01"
    calories: number;  // The total calories consumed on that day
  }
  